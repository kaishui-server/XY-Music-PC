/**
 * 应用版本号 —— 全项目唯一版本号源头
 *
 * 修改版本号时只需修改此处的 APP_VERSION，
 * 然后运行 `npm run version` 即可自动同步到：
 *   - package.json
 *   - src-tauri/tauri.conf.json
 *   - src-tauri/Cargo.toml
 *   - src-tauri/Cargo.lock
 *
 * 前端代码可直接 import { APP_VERSION } from '../version' 使用。
 */
export const APP_VERSION = '1.1.3-beta8';
