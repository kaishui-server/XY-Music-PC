<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinUIMusicPlayer.View.FavouritePlayListPage"
    xmlns:tools="using:WinUIMusicPlayer.Utils"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:WinUIMusicPlayer.Behaviors"
    xmlns:converters="using:WinUIMusicPlayer.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dev="using:DevWinUI"
    xmlns:extensions="using:WinUIMusicPlayer.Extensions"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:WinUIMusicPlayer.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:WinUIMusicPlayer.Model"
    xmlns:tool="using:WinUIMusicPlayer.Utils"
    mc:Ignorable="d">

    <Page.Resources>
        <Style
            x:Key="ListViewItemStyle"
            BasedOn="{StaticResource DefaultListViewItemStyle}"
            TargetType="ListViewItem">
            <Setter Property="ContextFlyout">
                <Setter.Value>
                    <MenuFlyout extensions:MenuFlyoutExtensions.ItemsSource="{x:Bind ViewModel.MenuOptions, Mode=OneWay}" />
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid RowDefinitions="Auto,*">
        <!--  顶部工具栏: 收藏数 + 播放全部按钮  -->
        <StackPanel
            Grid.Row="0"
            Margin="12,8,12,4"
            Orientation="Horizontal"
            Spacing="12">
            <Button
                x:Name="PlayAllButton"
                VerticalAlignment="Center"
                Click="PlayAllButton_Click"
                Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE768;" />
                    <TextBlock x:Name="PlayAllButtonText" />
                </StackPanel>
            </Button>
        </StackPanel>
        <ListView
            x:Name="MusicListView"
            Grid.Row="1"
            AllowDrop="True"
            CanDragItems="True"
            CanReorderItems="True"
            DoubleTapped="MusicListView_DoubleTapped"
            ItemContainerStyle="{StaticResource ListViewItemStyle}"
            ItemsSource="{x:Bind ViewModel.AppViewModel.FavoriteSongs, Mode=TwoWay}"
            RightTapped="MusicListView_RightTapped"
            SelectedItem="{x:Bind ViewModel.SelectedMusic, Mode=TwoWay}"
            SelectionMode="Extended">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="model:Music">
                <Grid ColumnDefinitions="Auto,Auto,3*,1.5*,3*,0.5*,70,Auto,50">
                    <Button
                        x:Name="CoverThumbnail"
                        Grid.Column="0"
                        Margin="0,5,10,5"
                        Padding="0"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        Command="{x:Bind PlayCommand, Mode=OneWay}"
                        CommandParameter="{x:Bind}"
                        CornerRadius="5"
                        Style="{StaticResource PlayHoverButtonStyle}">
                        <Grid Width="40" Height="40">
                            <Image
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Canvas.ZIndex="1"
                                Stretch="UniformToFill">
                                <interactivity:Interaction.Behaviors>
                                    <behaviors:AlbumCoverBehavior Music="{x:Bind Mode=OneWay}" />
                                </interactivity:Interaction.Behaviors>
                            </Image>
                            <Grid
                                Width="40"
                                Height="40"
                                Background="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
                                Canvas.ZIndex="0">
                                <FontIcon
                                    Canvas.ZIndex="0"
                                    FontSize="15"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Glyph="&#xE93C;" />
                            </Grid>
                        </Grid>
                    </Button>
                    <Button
                        x:Name="isFavouriteIconButton"
                        Grid.Column="1"
                        Margin="0,5,5,5"
                        Padding="3"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        Command="{x:Bind UpdateFavouriteCommand, Mode=OneWay}"
                        CommandParameter="{x:Bind}">
                        <FontIcon
                            x:Name="IsFavouriteIcon"
                            Foreground="Red"
                            Glyph="{x:Bind tools:BindUtils.FavouriteGlyphConverter(IsFavorite), Mode=OneWay}" />
                    </Button>
                    <TextBlock
                        Grid.Column="2"
                        Margin="5"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Text="{x:Bind Title, Mode=OneWay}"
                        TextTrimming="CharacterEllipsis" />
                    <Button
                        Grid.Column="3"
                        Margin="5"
                        Padding="5"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0">
                        <Button.Content>
                            <dev:AutoScrollView
                                Margin="0"
                                IsPlaying="False"
                                PointerCanceled="AutoScrollHover_PointerCanceled"
                                PointerEntered="AutoScrollHover_PointerEntered"
                                PointerExited="AutoScrollHover_PointerExited">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    Tapped="AuthorTextBlock_Tapped"
                                    Text="{x:Bind Author, Mode=OneWay}"
                                    TextTrimming="CharacterEllipsis" />
                            </dev:AutoScrollView>
                        </Button.Content>
                    </Button>
                    <Button
                        Grid.Column="4"
                        Margin="5"
                        Padding="5"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0">
                        <Button.Content>
                            <dev:AutoScrollView
                                Margin="0"
                                IsPlaying="False"
                                PointerCanceled="AutoScrollHover_PointerCanceled"
                                PointerEntered="AutoScrollHover_PointerEntered"
                                PointerExited="AutoScrollHover_PointerExited">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    Tapped="AlbumTextBlock_Tapped"
                                    Text="{x:Bind Album, Mode=OneWay}"
                                    TextTrimming="CharacterEllipsis" />
                            </dev:AutoScrollView>
                        </Button.Content>
                    </Button>
                    <Border
                        Grid.Column="5"
                        Padding="4,0,4,1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="#F7C8AC"
                        CornerRadius="2"
                        Visibility="{x:Bind tool:BindUtils.GetHrMusicVisibility(SampleRate, BitDepth), Mode=OneWay}">
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="9"
                            FontWeight="Bold"
                            Foreground="#F98555"
                            Text="HQ" />
                    </Border>
                    <TextBlock
                        Grid.Column="6"
                        Margin="5"
                        VerticalAlignment="Center"
                        Text="{x:Bind tools:BindUtils.TimeSpanToTextConverter(Duration), Mode=OneWay}" />
                    <Button
                        Grid.Column="7"
                        Margin="5"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        Command="{x:Bind AddMusicToCurrentPlayListCommand, Mode=OneWay}"
                        CommandParameter="{x:Bind}">
                        <FontIcon
                            Margin="0,2,0,0"
                            VerticalAlignment="Center"
                            Glyph="&#xE710;" />
                    </Button>
                    <FontIcon
                        x:Name="IsExistOnDeviceIcon"
                        Grid.Column="8"
                        FontSize="14"
                        Glyph="{x:Bind tools:BindUtils.IsExistOnDeviceGlyphConverter(IsExistOnDevice), Mode=OneWay}" />
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
